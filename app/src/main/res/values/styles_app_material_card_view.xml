<resources>

    <style name="Theme.MobileNews.MaterialCardView" parent="Widget.MaterialComponents.CardView"/>

    <style name="Theme.MobileNews.MaterialCardView.Default">
        <item name="cardCornerRadius">@dimen/_8sdp</item>
        <item name="cardElevation">@dimen/_2sdp</item>
        <item name="cardBackgroundColor">@color/card_background_color</item>
    </style>

</resources>
